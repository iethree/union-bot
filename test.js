const {
  getRandomBreakReminder,
  getRandomStartReminder,
  getTimeToBreak,
  getTimeToStart,
  getBreakMsg,
  getStartMsg,
} = require('./utils.js');

console.log(getStartMsg());
console.log('\n');
console.log(getBreakMsg());
